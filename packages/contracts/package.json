{
  "name": "@farmshare/contracts",
  "version": "1.0.0",
  "description": "Smart contracts for FarmShare Africa",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "node": "npx hardhat node",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network hedera-testnet",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network hedera-mainnet",
    "clean": "npx hardhat clean"
  },
  "keywords": ["blockchain", "farming", "defi", "hedera"],
  "author": "FarmShare Africa Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@hashgraph/sdk": "^2.40.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
